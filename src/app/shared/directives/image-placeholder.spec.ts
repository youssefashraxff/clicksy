import { ImagePlaceholder } from './image-placeholder';

describe('ImagePlaceholder', () => {
  it('should create an instance', () => {
    const directive = new ImagePlaceholder();
    expect(directive).toBeTruthy();
  });
});
